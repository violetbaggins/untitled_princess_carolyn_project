<!DOCTYPE html>
<html lang="en">
<!-- Include Head -->
<%- include('partials/head', {title: 'Tabla de posiciones'}) %>
<body>

<div class="container">

    <% if (locals.user){ %> 
        <!-- busca el resultado del usuario que lo esta usando -->
        <% result.forEach( (user , index) => {%> 
            <% if (user.id == locals.user) {%>
                <h1>✨Felicitaciones <span class="nombre"><%= user.name %> ✨</span></h1>
              
                <table>
                    <tr>
                        <th>POSICION</th>
                        <th>NOMBRE</th>
                        <th>PUNTAJE</th>
                    </tr>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td>⭐ <%= user.name %></td>
                        <td><%= user.total %></td>
                    </tr>
                   
                </table>
            <% }%> 
        <% })%> 
        <br>
        <!-- MUestra todos los otros usuarios -->

        
        <h2>Comparate con el resto, dale, yo se que querias saber</h2>
        <br>
        <table>
            <tr>
                <th class="blue">POSICION</th>
                <th class="blue">NOMBRE</th>
                <th class="blue">PUNTAJE</th>
            </tr>
            <% result.forEach( (user, index) => {%> 
                <tr>
                    <% if (index < 10){ %> 

                        <td><%= index + 1 %></td>
                        <% if (user.id == locals.user) {%>
                            <td>⭐ <%= user.name %></td>
                        <% } else {%>
                            <td><%= user.name %></td>
                        <% }%>
                        <td><%= user.total %></td>

                    <% } %> 
                </tr>
            <% })%> 
        </table>
        <br>
    <% } else { %> 
        <h1>No se pueden mostrar los totales</h1>
        <p>Capaz, porque en algun momento refrescaste,</p>
        <p>o trataste de saltearte algun paso...</p>
        <p>Yo que vos empiezo otra vez</p>
        <p>No me gustaría no figurar en "EL RANKING"</p>
    <% } %> 

   <hr>
   <h3>Si no dejaste mensaje, y mucho menos un mail podes seguirme en <a href="https://www.instagram.com/violetbaggins/">instagram</a></h3>
   <p>Aunque lo tengo privado, pero manda un mensaje asi se de donde venis! 😬 y si no, Telegram @violetbaggins </p>
   
</div>

<%- include('partials/footer') %>

</body>
</html>